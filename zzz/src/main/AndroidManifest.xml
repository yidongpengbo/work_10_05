<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bawei.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sector_graph.SectorGraphActivity" />
        <activity android:name=".turntable.TurntableActivity" />
        <activity android:name=".switch_button.SwitchButtonActivity" />
        <activity android:name=".random.RandomActivity" />
        <activity android:name=".flow.FlowActivity" />
        <activity android:name=".scrollandlist.ScrollAndListActivity" />
        <activity android:name=".weektest.WeekTestActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"></activity>
    </application>

</manifest>